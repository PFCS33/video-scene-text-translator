# Session: Poster — 2026-04-27

## Completed
- **Mockup iteration on `poster_mockup_final.svg`**: switched headline
  from "Bridging the gap between translation and realism" to
  "Replacing scene text inside video, across languages"; rewrote the
  motivation opening from report §1; replaced the bottom *Future Work*
  panel with *What the results show* (drawn from report §4.2 + §4.6).
- **Verified STRIVE citation**: confirmed it's **ICCV 2021** (not
  CVPR), and that the report's `references.bib` cite-key
  `subramanian2021strive` has the wrong first author — real first
  author is "Vijay Kumar B G". Updated poster files to cite as
  *"STRIVE (ICCV 2021)"* (option 2). Spawned a follow-up task to fix
  the report's bib entry.
- **Layout exploration**: produced four mockup variants
  (`poster_mockup_v2.svg` results-right, `v3` empty MOTIVATION +
  Main CONTRIBUTION blocks per teammate's PPT layout, `v4` model
  table moved under the pipeline diagram, then takeaway strip
  dropped per user request).
- **Real PPTX generated**: `poster/build_poster.py` (python-pptx,
  36"×24") + visual QA via LibreOffice → PDF → JPG. Two iterations
  to fix tagline overflow and LCM figure mapping.
- **Poster polished in PowerPoint** (user-side) and exported to
  `poster/poster.pdf` (12.7 MB, 36"×24" landscape) — the submitted
  artifact.
- **Email reply drafted** to Nina (MPCS Coordinator) with project
  title, abstract, group members, and attached poster filename
  (`CMPT743_CrossLanguageVideoTextReplacement_Poster.pdf`).
- **Repo cleanup + commits on `feat/final-report`**:
  - `37f9221` *feat(poster): add MPCS Innovation Prize poster (final submission)*
  - `3a518bf` *chore(poster): drop the two logo concepts*
  - `.gitignore` extended for Office lock files (`~$*.pptx`) and
    drawio scratch files (`.$*.drawio*`).

## Current State
- Branch `feat/final-report` at `3a518bf`, **not yet pushed**.
- Worktree clean.
- `poster/` final contents: `poster.pdf` · `build_poster.py` ·
  `template_mpcs_project_poster.pptx` · `figures/{pipeline.drawio,
  pipeline.png}` · `refs/{CMPT743_SceneTextTranslation_Report.pdf,
  project_summary.md}`.
- Two intermediate-session sessions archived:
  `docs/sessions/poster-2026-04-20.md`, this one.
- Spawned follow-up: fix STRIVE bib in `report/references.bib`
  (wrong first author + author list).

## Next Steps
1. `git push` to origin (then ship the email + poster PDF to Nina).
2. Present in person at MPCS Innovation Prize on Tuesday April 28
   (12:00–3:30pm PST, SFU Vancouver Harbour Centre).
3. After the competition: merge `feat/final-report` → `master`,
   then run the spawned bib-fix task.

## Decisions Made
- **Title framing.** Section headline rewritten to *"Replacing scene
  text inside video, across languages"* — concrete and echoes the
  paper title without exactly duplicating the poster's top bar.
- **STRIVE citation form.** Use *"STRIVE (ICCV 2021)"* on the poster
  (option 2 of three). Skips the awkward first-author name format
  ("Kumar B G") while staying technically correct.
- **Contribution-table heading.** Rejected user's *"Heavy
  Engineering"* in favour of *"Engineering contribution — 7
  integrated models, 2 trained by us"*. "Heavy" reads as marketing
  to an academic audience; the numbers carry the depth signal.
- **Layout.** Final structure = motivation top band (left+center),
  Results column right (untouched from teammate's v1), pipeline
  diagram center with model table directly below (uses up otherwise
  empty space below the diagram's 16:9 footprint). Bottom
  takeaway strip dropped — the takeaway lives implicitly in the
  contribution table + Results captions.
- **Logo concepts dropped.** Two SVG explorations
  (`logo_A_frame.svg`, `logo_B_mark.svg`) removed in `3a518bf`;
  not used on the final poster.

## Open Questions
- None blocking the competition. Long-term: fix the report's
  STRIVE bib entry (queued as a separate background task).
