# Session: Poster — 2026-04-20

## Completed
- Stood up `poster/` working directory (still untracked) holding the
  SFU MPCS PPT template, a teammate-supplied v1 PDF draft
  (`refs/CMPT743_SceneTextTranslation_Report.pdf`), a
  `project_summary.md` crib sheet, and the report PDF for reference.
- Drafted `poster/poster_content.md` — full block-by-block copy for a
  36"×24" landscape poster: title bar, motivation (4-axes pill row +
  generative-vs-ours table), pipeline hero, five per-stage blocks,
  BPN / Alignment Refiner / LCM module cards, Model-Involvement table
  (seven models, two ours), Results strip, Takeaway + Future Work,
  footer. Tone matches report content_brief §0.5 (prototype /
  feasibility / reproducible baseline — no "novel" / "SOTA").
- Authored `poster/figures/pipeline.drawio` (+ rendered PNG) — dense
  internal-dataflow diagram showing Hello→你好 through
  detect/frontalize/edit/propagate/revert.
- Produced `poster/poster_mockup.svg` as a layout reference for
  whoever arranges the PPT.
- Dropped the generative-vs-adaptive (F) block — WARDEN→典狱长 triplet
  deemed not a strong enough example; its slot is now the
  Model-Involvement table.
- Listed v1-draft cleanup items (stray PPT comment bubble, LCM figure
  overlap, pink sticky note, thin Results body, orphaned BPN figure)
  in `poster_content.md` "Things to clean up" appendix.

## Current State
- Branch `feat/final-report` at `73b3729`, pushed. Final report
  submitted earlier today (separate session).
- `poster/` is entirely **untracked** — nothing committed yet. No
  poster branch yet; TBD whether to land on `feat/final-report` or a
  new `feat/poster` branch.
- Poster diagram asset lives at `poster/figures/pipeline.drawio` +
  `pipeline.png`; module figures are reused from `report/figures/`
  (bpn.png, refiner-network.png, lcm-result.jpg, results_overview.png).
- Open PPT file (`~$template_mpcs_project_poster.pptx`) means the
  template is still open in Keynote/PowerPoint.

## Next Steps
1. Decide branch strategy — new `feat/poster` off master, or keep on
   `feat/final-report`. Add `.gitignore` for `.DS_Store`, `~$*.pptx`,
   and `.$*.drawio.bkp/.dtmp` before committing.
2. Arrange the PPT: pour `poster_content.md` blocks into the template,
   following the cleanup checklist at the bottom of that file.
3. Resolve **open decision #11** — drawio dense diagram vs. report's
   cleaner S1–S5 boxes as the hero. Revisit after a test print.
4. Generate / drop in `poster/figures/qr_github.png` once the final
   GitHub URL is confirmed.
5. Proofread at print scale (≥2 m viewing distance), then export PDF
   for submission.

## Decisions Made
- **Drop §F (generative-vs-adaptive) from poster.** WARDEN→典狱长
  triplet isn't visually decisive; Model-Involvement table takes the
  slot and sells the ownership story ("two of seven are ours").
- **Poster tone matches report content_brief §0.5** — no "novel" or
  "SOTA" language. "Reproducible open baseline." Complementary to —
  not competing with — generative editors.
- **Per-stage blocks stay short** — one paragraph each; details live
  in module cards (BPN / Refiner / LCM) on the right column.
- **Bold "trained from scratch" / "re-implementation"** so viewers
  scanning can immediately see where our engineering lives.

## Open Questions
- **#11** — drawio dataflow diagram vs. report's abstract S1–S5 boxes
  for the hero. Leaning drawio because it literally shows
  Hello→你好; to be revisited after a test print.
- Who owns the PPT arrangement pass — is Liliana (v1 draft author)
  taking the markdown + cleanup list, or are we doing it?
- Final GitHub repo URL for the QR — confirm before the QR is
  generated.
